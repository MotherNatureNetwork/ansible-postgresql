---
driver:
  name: docker

docker:
  containers:

    - name: mnn.postgresql-01
      image: ubuntu
      image_version: trusty
      ansible_groups:
        - group1

ansible:
  playbook: tests/playbook.yml
  requirements_file: tests/requirements.yml

verifier:
  name: testinfra
